# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo
plugin.zip
koan-plugin.zip

# Secrets & local config — NEVER commit these
.env
.env.local
.env.*.local
.mcp.json
.claude/settings.local.json

# Local knowledge store (lives in ~/.koan, not the repo)
knowledge/

# Python (legacy prototype leftovers)
__pycache__/
*.pyc

# OS
.DS_Store
