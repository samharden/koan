---
name: scaffold
description: >-
  Turn a promoted knowledge unit into a runnable Claude skill — the inverse of
  capture. Reads a unit's steps, exceptions, and authorities and generates a
  SKILL.md the firm can invoke as a workflow in Cowork / Claude Desktop / the
  CLI. Use when the user wants to OPERATIONALIZE captured knowledge: "turn this
  into a skill", "make this a workflow", "scaffold a skill from X", "deploy this
  process", "make this runnable", or invokes /koan:scaffold. Also handles drift:
  "are my scaffolded skills up to date", "check for stale skills", "regenerate
  the skills whose units changed" (the --check mode). Only scaffolds promoted
  units (never drafts); it generates a skill file, it does not change any unit in
  the store.
---

# Scaffold a workflow from captured knowledge

`capture` turns a process into stored **knowledge** (a `Unit` — descriptive: what
the firm does). This skill does the reverse: it turns a promoted unit into a
**workflow** — an invokable skill that walks the steps in a live session. This is
the "deploy" half of the loop: capture → review → recall, then **scaffold** the
ones worth running.

The output is a generated `SKILL.md`. This skill **does not** modify the store —
it reads a unit and writes a new skill file from it.

It has two modes:
- **Generate** (default) — produce or refresh a workflow skill from a unit.
- **Check** (`--check`, or "are my skills up to date") — compare every scaffolded
  skill against its source unit and regenerate the ones that have drifted.

## Snapshot + drift model

A generated skill is a **snapshot**: the steps are baked in so it runs anywhere on
its own (that portability is the point). To keep snapshots honest, each one carries
a `koan_source` fingerprint and is tracked in a manifest:

- **Fingerprint = the source unit's `verified_on` date.** That date advances every
  time a human (re-)promotes the unit — i.e. on the *vouched* change. So a
  scaffolded workflow tracks **certified** changes, not arbitrary in-place edits;
  an unreviewed edit shouldn't silently propagate into a deployed workflow anyway.
- **Manifest** at `<knowledge folder>/skills/.scaffold-manifest.json` (default
  `~/Documents/firm-knowledge/skills/.scaffold-manifest.json`) maps each source
  unit id → the generated skill(s) and the `verified_on` they were built from, so
  `--check` knows what exists and whether it's stale.

## When to use

- "Turn our conflict-waiver playbook into a skill." / "Make this runnable."
- "Scaffold a workflow from the intake unit."
- "Deploy this process so the team can just run it."

For *retrieving* knowledge in conversation use `recall`; for *writing it down*
use `capture`. This skill is specifically about producing a reusable, invokable
workflow artifact from knowledge that already exists and has been vouched for.

## Workflow

1. **Pick the source unit — promoted only.** `Glob` `units/*.md` and `inbox/*.md`
   in the knowledge folder (default `~/Documents/firm-knowledge/`) and select the
   unit the user means. It **must** be a promoted unit (a file in `units/`,
   `status: active`), not a draft (in `inbox/`). If the user points at a draft, stop
   and explain: drafts aren't authoritative, so they shouldn't become runnable
   workflows — send them to `/koan:review` to promote it first.
2. **Read it in full.** `Read` the unit file to get every field — you need `title`,
   `trigger`, `steps`, `exceptions`, `authorities`, `templates`,
   `confidentiality`, and `open_questions` to generate a faithful skill.
3. **Confirm scope with the user**, especially:
   - **Open questions.** If the unit still has unresolved `open_questions`, surface
     them — a workflow that bakes in an unsettled step is risky. Offer to proceed
     with a noted caveat, or to resolve them in `review` first.
   - **Confidentiality.** If the unit is `walled` or `client`, the generated skill
     will embed client-specific or screened procedure. Warn the user and confirm
     *where* it will be installed (a personal/firm skills dir, not a shared or
     public one). Don't scaffold a `client` unit without explicit confirmation.
4. **Generate the SKILL.md** by mapping the unit's fields:

   ```markdown
   ---
   name: <kebab-case of unit title>
   description: >-
     <One paragraph in "Use when…" form, built from the unit's trigger and
     title, so it triggers on the right requests. Name concrete phrasings.>
   koan_source:
     id: <source unit id>
     verified_on: <the unit's verified_on date — the drift fingerprint>
     scaffolded_on: <today's date>
   ---

   # <Unit title>

   <One line: what this workflow does. Then a provenance line —
   "Generated by koan scaffold from unit <id>, verified by <verified_by> on
   <verified_on>. Regenerate with /koan:scaffold if the source unit is
   re-promoted.">

   ## When to use
   <From the unit's `trigger` — the situation that fires this workflow.>

   ## Workflow
   <Numbered list from `steps`. Keep them as actionable instructions, not a
   summary. Preserve order.>

   ## Watch out for
   <From `exceptions` — the traps, edge cases, and "don'ts". Omit the section
   if the unit has none.>

   ## Authorities & references
   <From `authorities` and `templates`. Omit if empty.>

   ## Guardrails
   <Always include: this workflow follows captured firm procedure; if a
   situation falls outside the steps or an exception applies, stop and escalate
   rather than improvise. If the source unit is walled/client, add a
   confidentiality line. If open_questions were unresolved, note them here as
   known gaps.

   Also include this friction line: "Friction — if this run hit a situation
   these steps didn't cover (an escalation, a missing step, an exception not
   listed above), and the knowledge folder is available, append one line to
   `friction.md` at its root:
   `- <YYYY-MM-DD> — skill <slug> (unit <id>) — hit: \"<what the steps didn't
   cover>\"` (create the file if absent). Keep the description generic — no
   client names or matter details. This is how the deployed workflow reports
   back so the source unit can be improved; if the folder isn't available,
   tell the user what to capture instead."

   Also include this freshness line: "Freshness — this is a snapshot of unit
   <id> as of <verified_on>. If the knowledge folder is available, you may Read
   `units/<id>.md` and check that its verified_on still matches the
   koan_source.verified_on in this file's frontmatter; if it has advanced, the
   unit was re-promoted and this skill should be regenerated with /koan:scaffold.
   Do not block the workflow on this — it is a self-heal hint, not a dependency.">
   ```

5. **Write the file and record it in the manifest.** Write the skill to
   `<knowledge folder>/skills/<slug>/SKILL.md` (default
   `~/Documents/firm-knowledge/skills/<slug>/SKILL.md`). Then read
   `<knowledge folder>/skills/.scaffold-manifest.json` (create `{ "skills": [] }`
   if absent) and upsert an entry — `{ slug, path, source_id, verified_on,
   scaffolded_on, installed_at? }` — keyed by `slug`, so `--check` can find it
   later. Show the
   user the generated content and the path. Don't overwrite an existing generated
   skill without asking.
6. **Offer to install it.** The generated skill is a normal Claude skill; to be
   invokable it must live somewhere Claude loads. Offer the two destinations —
   ask which (or both) fits how this workflow will be used:
   - **Just for this user** → copy the `<slug>/` folder to
     `~/.claude/skills/<slug>/` (personal skills — loaded by Cowork, Desktop,
     and the CLI alike). Don't overwrite an existing `~/.claude/skills/<slug>/`
     you didn't generate.
   - **For the whole firm** → add it to the **firm plugin** (see below), the
     single shareable artifact the rest of the team installs once. This is the
     right default for any procedure more than one person runs — but **never**
     for a `walled` / `client` unit (see Guardrails).
   Confirm the destination first — especially for a `walled` / `client` unit,
   where the user must confirm the location is private. After installing,
   restart the surface (or `/reload-plugins` in the CLI); the skill then
   triggers on its description or `/<slug>`. Record every installed copy's path
   in the manifest entry (`installed_at`, a list if both), so a `--check`
   regeneration can refresh the installed copies, not just the one in the
   knowledge folder.

## The firm plugin — deploy once, run everywhere

Scaffolding to one person's laptop doesn't operationalize a firm process. The
firm plugin is a normal Claude plugin folder that collects the firm's
scaffolded workflow skills so the team installs **one** thing and gets all of
them — and future scaffolds/regenerations flow through the same channel.

It lives at `<knowledge folder>/firm-plugin/` (default
`~/Documents/firm-knowledge/firm-plugin/`):

```
firm-plugin/
├── .claude-plugin/plugin.json    ← the plugin manifest
└── skills/<slug>/SKILL.md        ← one folder per scaffolded skill
```

- **First time**: create the folder and write `plugin.json` — ask the user for
  the plugin name (suggest a kebab-case of the firm's name + `-workflows`,
  e.g. `harden-llp-workflows`) and use a description like "Our firm's
  scaffolded workflow skills — generated by Koan from promoted knowledge
  units." Version it `0.1.0` and bump the patch version on every later add or
  regeneration, so installs are distinguishable.
- **Adding a skill**: copy the generated `<slug>/` folder into
  `firm-plugin/skills/<slug>/` and record that path in the manifest entry's
  `installed_at`.
- **Distributing**: the folder is shared like the knowledge folder itself —
  it's already inside it, so a synced drive shares it automatically, or the
  firm keeps it in a git repo. Each person installs it once: in Cowork /
  Desktop, upload a zip of the folder's **contents** (manifest at the zip
  root — same rule as installing Koan itself, `cd firm-plugin && zip -r
  ../firm-skills.zip . -x '*.DS_Store'`); in the CLI,
  `claude plugin marketplace add /path/to/firm-plugin` works directly on the
  folder. After a regeneration, teammates re-install/update the same way —
  tell the user this is the moment to announce it.
- **Confidentiality**: the firm plugin is firm-wide by definition. `internal`
  units belong; `walled` / `client` units **never** go in it — those install
  personally, to the people inside the wall, or not at all.

## Checking for drift (`--check`)

When the user asks whether their scaffolded skills are current (or invokes
`/koan:scaffold --check`):

1. **Load the manifest** at `<knowledge folder>/skills/.scaffold-manifest.json`
   (default `~/Documents/firm-knowledge/skills/.scaffold-manifest.json`). If it's
   missing or empty, say nothing has been scaffolded yet and stop.
2. **Compare each entry to its source unit.** For every entry, `Read` the source
   unit (`units/<source_id>.md`) and compare the unit's current `verified_on` to
   the entry's stored `verified_on`:
   - **Match** → up to date; no action.
   - **Advanced** → the unit was re-promoted since scaffolding; the skill is
     **stale**.
   - **Unit retired** (now in `archive/`, `status: retired`) → the firm no longer
     stands behind this procedure, so the deployed skill has lost its backing.
     Say so, name the `retired_on` date, and — if the archived unit has a
     `superseded_by` — offer to scaffold the replacement unit instead. Recommend
     removing the old skill; don't delete anything yourself.
   - **Unit missing / now a draft** → flag it: the source was pulled or demoted,
     so the deployed skill no longer has authoritative backing. Recommend the user
     remove or re-scaffold it; don't delete anything yourself.
3. **Report drift plainly** — list stale skills with old vs current `verified_on`,
   most-changed first.
4. **Regenerate on the user's say-so.** For each stale skill they approve, re-run
   the Generate workflow above against the current unit (overwriting that skill's
   file and updating its manifest `verified_on`). If the manifest entry has
   `installed_at` path(s), refresh every installed copy too — a regenerated
   snapshot that never reaches the installed skill is still stale where it
   counts. When a refreshed copy lives in the firm plugin, bump the plugin's
   patch version and remind the user that teammates need to re-install to get
   it. Confirm before overwriting; honor the same confidentiality and
   open-question checks as a fresh scaffold.

## Guardrails

- **Promoted only.** Never scaffold from a draft. A runnable workflow implies the
  firm stands behind it; that's exactly what promotion certifies.
- **Don't touch the store.** This skill only *reads* unit files and *writes a new
  skill file* (plus the scaffold manifest). It never edits, moves, promotes, or
  deletes a unit.
- **Faithful, not embellished.** The generated steps must come from the unit. Don't
  add procedure the firm never captured, and don't drop exceptions to make the
  workflow look cleaner — the exceptions are often the whole point.
- **Snapshot, with provenance.** The generated skill is a point-in-time copy.
  Always stamp the `koan_source` fingerprint (id + `verified_on` + `scaffolded_on`)
  and update the manifest, so drift is detectable and the snapshot is traceable.
  The fingerprint is `verified_on`, not a content hash — drift means the unit was
  **re-promoted**, which is the only change a deployed workflow should track.
- **`--check` is read + regenerate only.** It reads units and (with approval)
  rewrites generated skill files and the manifest. It never edits, promotes, or
  rejects units, and never deletes a skill without the user asking.
- **Respect confidentiality at install time.** A `walled` / `client` unit's
  procedure should only be scaffolded into a private skills location the user
  confirms — never somewhere shared or public, and **never into the firm
  plugin**, which is shared with the whole firm by design.
